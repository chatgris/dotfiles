[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
[alias]
    a   = add
    b   = branch
    ba  = branch -a
    bc  = branch --contains
    bm  = branch --merged
    bn  = branch --no-merged
    ch  = cherry-pick
    ci  = commit
    co  = checkout
    ds  = describe
    df  = diff --word-diff
    dc  = diff --word-diff --cached
    dcs = diff --word-diff --cached --stat
    ds  = diff --stat
    fe  = fetch -p -a
    ft  = fetch -t
    i   = remote -v
    l   = log
    lf  = log --follow
    lo  = log -p
    lof = log -p --follow
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    pl  = pull --rebase
    ph  = push
    sh  = show
    sm  = submodule update
    st  = status -sb
    stm = diff --name-status --diff-filter=U
[push]
    default = current
[github]
    user = chatgris
[rerere]
    enabled = 1
[user]
    email = julien@stormz.com
    name = chatgris
[commit]
  template = ~/.git-commit-template
[gh]
  autoUpdate = always
