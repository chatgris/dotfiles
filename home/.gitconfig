[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
[alias]
    a   = add
    b   = branch
    ba  = branch -a
    bc  = branch --contains
    bm  = branch --merged
    bn  = branch --no-merged
    ch  = cherry-pick
    ci  = commit
    cia = commit --amend
    co  = checkout
    ds  = describe
    df  = diff --word-diff
    dfs = diff --word-diff --stat
    dc  = diff --word-diff --cached
    dcs = diff --word-diff --cached --stat
    ds  = diff --stat
    fe  = fetch -p -a
    ft  = fetch -t
    i   = remote -v
    l   = log
    lf  = log --follow
    lo  = log -p
    lof = log -p --follow
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    mf  = merge --no-ff -
    pl  = pull --rebase
    ph  = push
    phf = push -f
    rd  = rebase develop
    sh  = show
    sm  = submodule update
    st  = status -sb
    stm = diff --name-status --diff-filter=U
[push]
    default = current
[github]
    user = chatgris
[rerere]
    enabled = 1
[user]
    email = julien@stormz.me
    name = chatgris
[commit]
  template = ~/.git-commit-template
[gh]
  autoUpdate = always
